# ============================================================================
# MCP (Model Context Protocol) Integration Environment Variables
# ============================================================================
# Copy this file to .env and fill in your values
# NEVER commit the actual .env file to version control

# ============================================================================
# REQUIRED - Core MCP Configuration
# ============================================================================

# Encryption key for OAuth tokens (32 characters minimum)
# Generate with: openssl rand -base64 32
MCP_ENCRYPTION_KEY=CHANGE_THIS_TO_A_SECURE_32_CHAR_KEY

# ============================================================================
# OPTIONAL - Tool Integrations
# Only add the tools you want to integrate
# ============================================================================

# ----------------------------------------------------------------------------
# GitHub Integration
# ----------------------------------------------------------------------------
# Create OAuth App: https://github.com/settings/applications/new
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/github

# ----------------------------------------------------------------------------
# Jira Integration (Atlassian)
# ----------------------------------------------------------------------------
# Create App: https://developer.atlassian.com/console/myapps/
JIRA_CLIENT_ID=
JIRA_CLIENT_SECRET=
JIRA_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/jira

# ----------------------------------------------------------------------------
# Figma Integration
# ----------------------------------------------------------------------------
# Create App: https://www.figma.com/developers/apps
FIGMA_CLIENT_ID=
FIGMA_CLIENT_SECRET=
FIGMA_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/figma

# ----------------------------------------------------------------------------
# Microsoft Outlook Integration
# ----------------------------------------------------------------------------
# Register App: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
OUTLOOK_CLIENT_ID=
OUTLOOK_CLIENT_SECRET=
OUTLOOK_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/outlook

# ----------------------------------------------------------------------------
# Confluence Integration (Atlassian)
# ----------------------------------------------------------------------------
# Same as Jira - use Atlassian OAuth
CONFLUENCE_CLIENT_ID=
CONFLUENCE_CLIENT_SECRET=
CONFLUENCE_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/confluence

# ----------------------------------------------------------------------------
# Slack Integration
# ----------------------------------------------------------------------------
# Create App: https://api.slack.com/apps
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
SLACK_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/slack

# ----------------------------------------------------------------------------
# Microsoft Teams Integration
# ----------------------------------------------------------------------------
# Can use same app as Outlook or create separate app
# Register App: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
# Required API permissions: Team.ReadBasic.All, Channel.ReadBasic.All, Chat.Read, ChannelMessage.Read.All
TEAMS_CLIENT_ID=
TEAMS_CLIENT_SECRET=
TEAMS_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/teams

# ============================================================================
# PRODUCTION CONFIGURATION
# ============================================================================
# For production, update all redirect URIs to use your domain:
# Example: https://yourdomain.com/api/v1/mcp/callback/github

# ============================================================================
# OPTIONAL - Debug & Development
# ============================================================================

# Enable MCP debug logging (development only)
MCP_DEBUG=false

# Session duration in minutes (default: 30)
MCP_SESSION_DURATION=30

# ============================================================================
# NOTES
# ============================================================================
# 1. All OAuth tokens are encrypted using AES-256 with the MCP_ENCRYPTION_KEY
# 2. No external data is stored - only encrypted OAuth tokens
# 3. Sessions auto-expire after MCP_SESSION_DURATION minutes
# 4. Each tool requires its own OAuth app configuration
# 5. Minimal scopes are requested for each tool
# 6. Users must explicitly consent before any data is fetched
# 7. Teams and Outlook can share the same Microsoft app registration
#    - If TEAMS_CLIENT_ID is not set, it will fallback to OUTLOOK_CLIENT_ID
# ============================================================================
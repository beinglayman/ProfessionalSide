# ============================================================================
# MCP (Model Context Protocol) Integration Environment Variables
# ============================================================================
# Copy relevant sections to your .env and fill in values.
# NEVER commit the actual .env file to version control.
#
# Quick setup: npm run oauth-cli -- setup
# Validate:    npm run oauth-cli -- validate
# ============================================================================

# ============================================================================
# REQUIRED - Core Configuration
# ============================================================================

# Encryption key for OAuth tokens (32 characters minimum)
# Generate with: openssl rand -base64 32
# Service accepts either name (checks ENCRYPTION_KEY first, then MCP_ENCRYPTION_KEY)
ENCRYPTION_KEY=CHANGE_THIS_TO_A_SECURE_32_CHAR_KEY

# Backend URL for OAuth callback construction (defaults to http://localhost:3002)
# BACKEND_URL=http://localhost:3002

# ============================================================================
# OPTIONAL - Tool Integrations
# Only add the tools you want to integrate.
# ============================================================================

# ----------------------------------------------------------------------------
# GitHub Integration
# ----------------------------------------------------------------------------
# Create OAuth App: https://github.com/settings/applications/new
# Callback URL: http://localhost:3002/api/v1/mcp/callback/github
# Scopes: repo read:user
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
# GITHUB_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/github

# ----------------------------------------------------------------------------
# Atlassian Integration (Jira + Confluence — shared credentials)
# ----------------------------------------------------------------------------
# Create App: https://developer.atlassian.com/console/myapps/
# Callback URLs (add ALL three):
#   http://localhost:3002/api/v1/mcp/callback/jira
#   http://localhost:3002/api/v1/mcp/callback/confluence
#   http://localhost:3002/api/v1/mcp/callback/atlassian
ATLASSIAN_CLIENT_ID=
ATLASSIAN_CLIENT_SECRET=
# JIRA_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/jira
# CONFLUENCE_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/confluence

# ----------------------------------------------------------------------------
# Google Workspace Integration (Drive, Calendar)
# ----------------------------------------------------------------------------
# 1. Configure consent screen: https://console.cloud.google.com/apis/credentials/consent
# 2. Create OAuth credentials: https://console.cloud.google.com/apis/credentials
# Callback URL: http://localhost:3002/api/v1/mcp/callback/google_workspace
# Scopes: userinfo.email, userinfo.profile, drive.readonly, calendar.readonly
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/google_workspace

# ----------------------------------------------------------------------------
# Microsoft Integration (Outlook, Teams, OneDrive, OneNote — shared credentials)
# ----------------------------------------------------------------------------
# Register App: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps
# Callback URLs (add ALL four):
#   http://localhost:3002/api/v1/mcp/callback/outlook
#   http://localhost:3002/api/v1/mcp/callback/teams
#   http://localhost:3002/api/v1/mcp/callback/onedrive
#   http://localhost:3002/api/v1/mcp/callback/onenote
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
# MICROSOFT_TENANT_ID=common
# OUTLOOK_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/outlook
# TEAMS_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/teams
# ONEDRIVE_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/onedrive
# ONENOTE_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/onenote

# ----------------------------------------------------------------------------
# Figma Integration
# ----------------------------------------------------------------------------
# Create App: https://www.figma.com/developers/apps
# Callback URL: http://localhost:3002/api/v1/mcp/callback/figma
FIGMA_CLIENT_ID=
FIGMA_CLIENT_SECRET=
# FIGMA_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/figma

# ----------------------------------------------------------------------------
# Slack Integration
# ----------------------------------------------------------------------------
# Create App: https://api.slack.com/apps
# Callback URL: http://localhost:3002/api/v1/mcp/callback/slack
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
# SLACK_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/slack

# ----------------------------------------------------------------------------
# Zoom Integration
# ----------------------------------------------------------------------------
# Create App: https://marketplace.zoom.us/develop/create
# Callback URL: http://localhost:3002/api/v1/mcp/callback/zoom
ZOOM_CLIENT_ID=
ZOOM_CLIENT_SECRET=
# ZOOM_REDIRECT_URI=http://localhost:3002/api/v1/mcp/callback/zoom

# ============================================================================
# OPTIONAL - Debug & Development
# ============================================================================

# Enable MCP debug logging (development only)
MCP_DEBUG=false

# ============================================================================
# NOTES
# ============================================================================
# 1. OAuth tokens are encrypted using AES-256 with the ENCRYPTION_KEY
# 2. Atlassian: one app registration covers both Jira and Confluence
# 3. Microsoft: one app registration covers Outlook, Teams, OneDrive, OneNote
# 4. Google: consent screen must be configured before creating credentials
# 5. Redirect URI overrides are optional — defaults use BACKEND_URL or localhost:3002
# 6. Run `npm run oauth-cli -- validate` to check your configuration
# ============================================================================

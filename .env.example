# InChronicle Environment Configuration
# Copy to .env.local and configure for your use case
# ============================================================
#
# TWO MODES OF OPERATION:
#
# MODE 1: Test against PRODUCTION (no local server needed)
#   - Playwright runs against https://inchronicle.com
#   - No need to start local dev server
#   - VITE_API_URL is NOT needed (not running local UI)
#
# MODE 2: Test against LOCAL UI (requires npm run dev)
#   - Playwright runs against http://localhost:5173
#   - Must start local dev server first: npm run dev
#   - VITE_API_URL points to production backend for real data
#
# ============================================================

# ------------------------------------------------------------
# MODE 1: Production Testing (recommended for screenshot verification)
# ------------------------------------------------------------
# Uncomment these lines to test against production:
#
# E2E_BASE_URL=https://inchronicle.com
# E2E_EMAIL=your-test-email@example.com
# E2E_PASSWORD=your-test-password
#
# Then run: npm run screenshots
# (No need to start local server)

# ------------------------------------------------------------
# MODE 2: Local UI Testing (for testing local UI changes)
# ------------------------------------------------------------
# Uncomment these lines to test local UI with production data:
#
# VITE_API_URL=https://ps-backend-1758551070.azurewebsites.net/api/v1
# E2E_BASE_URL=http://localhost:5173
# E2E_EMAIL=your-test-email@example.com
# E2E_PASSWORD=your-test-password
#
# Then run:
#   1. npm run dev          (start local UI pointing to prod API)
#   2. npm run screenshots  (in another terminal)

# ------------------------------------------------------------
# OPTIONAL: Real OAuth onboarding E2E suite
# ------------------------------------------------------------
# Enables dedicated real-provider onboarding tests:
#   npm run e2e:oauth:readiness
#   npm run test:e2e:onboarding:real
#
# E2E_REAL_OAUTH=true
# E2E_REAL_EMAIL=your-real-e2e-user@example.com
# E2E_REAL_PASSWORD=your-real-e2e-password
# E2E_REQUIRED_TOOLS=github,jira,confluence,google_workspace,outlook
# E2E_API_URL=http://localhost:3002/api/v1
